const userData = (state) => state.userData;
const roomId = (state) => state.roomId;
const preferredVideoData = (state) => state.preferredVideoData;
const messages = (state) => state.messages;

export {
  userData,
  roomId,
  preferredVideoData,
  messages,
};
